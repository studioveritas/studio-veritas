# Only needed if you wire the contact panel to the /api/contact route + Postgres.
# The current site just uses a mailto: link, so this can stay blank.
DATABASE_URL=""

# Used by app/layout.tsx for canonical URLs / Open Graph — set this once you
# know your production domain, e.g. https://studioveritas.com
NEXT_PUBLIC_SITE_URL=""
